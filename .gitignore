# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Runtime / generated data (safe to keep out of git)
chroma_db/
ollama_data/

# Real SQL dump at repo root (not committed); the bundled data/data.sql
# shipping the Magento catalog IS committed so the deploy is self-contained.
/data.sql

# OS / editor
.DS_Store
*.swp
.vercel
.env*
